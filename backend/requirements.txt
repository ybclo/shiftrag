# ShiftRAG — Backend dependencies (Colab-friendly — flexible versions)
# Core — no strict pins so Colab's Python 3.11/3.12/3.13 all work
fastapi
uvicorn[standard]
pydantic
pydantic-settings
python-multipart
aiofiles
python-dotenv
httpx
# Colab tunnel (optional — enables Google Colab free-GPU hosting)
pyngrok
nest-asyncio

# Parsing — primary + fallbacks
# docling is IBM's spatial document parser (layout, tables, OCR).
# It's large (~1GB with models) — install with care. Fallbacks work if omitted.
# docling — uncomment if you want full vision (optional, heavy):
# docling
pypdf
python-docx
openpyxl
python-pptx
pillow

# Embeddings — local, no API key (flexible so Colab finds a compatible wheel)
fastembed
onnxruntime

# Vector DB — in-memory default (no server needed)
qdrant-client

# Utils
numpy
